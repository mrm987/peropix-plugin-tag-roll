# 앱이 설치할 때 번들 pip 으로 `_lib/` 에 넣는다 (앱의 site-packages 는 건드리지 않는다).
# 색인(duckdb 파일·parquet)을 읽는 데 쓴다.
duckdb>=1.0.0
# ★numpy 는 적지 않는다 — 앱 백엔드가 이미 싣고 있고(`backend/requirements.txt`), `_lib` 는
#   sys.path 뒤에 붙으므로 여기 또 깔아도 앱의 것이 쓰인다. 같은 것을 두 벌 받게만 된다.
